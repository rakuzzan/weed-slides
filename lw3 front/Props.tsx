import { Colors } from "./Colors"

export type Props = {
    fillColor: Colors,
    strokeColor: Colors,
    strokeWigth: number,
    bold: boolean;
    italic: boolean;
    highlighted: boolean;
}